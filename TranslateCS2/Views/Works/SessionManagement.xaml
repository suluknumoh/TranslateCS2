<ContentControl
    x:Class="TranslateCS2.Views.Works.SessionManagement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    xmlns:app_configurationmanager="clr-namespace:TranslateCS2.Configurations"
    xmlns:i18n="clr-namespace:TranslateCS2.Properties.I18N"
    xmlns:sessionmodels="clr-namespace:TranslateCS2.Models.Sessions"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto"/>
            <RowDefinition
                Height="Auto"/>
            <RowDefinition
                Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Style="{StaticResource StyleHeading}"
            Text="{Binding Source={x:Static i18n:I18NSessions.Caption}, Mode=OneWay}"/>
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            TextAlignment="Center"
            FontSize="20"
            Foreground="DarkRed"
            TextWrapping="Wrap"
            Margin="10 10 10 10"
            Text="{Binding SessionManager.DatabaseError,
                           Mode=OneWay,
                           NotifyOnSourceUpdated=True}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter
                        Property="Visibility"
                        Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger
                            Binding="{Binding SessionManager.HasDatabaseError,
                                              Mode=OneWay,
                                              NotifyOnSourceUpdated=True}"
                            Value="True">
                            <Setter
                                Property="Visibility"
                                Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <ScrollViewer
            Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="*"/>
                    <ColumnDefinition
                        Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    TextAlignment="Right"
                    Margin="0 0 5 0"
                    TextWrapping="Wrap"
                    Text="{Binding Source={x:Static i18n:I18NSessions.OutputInstallationDirectory}, StringFormat={}{0}:, Mode=OneWay}"/>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="5 0 0 0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    TextAlignment="Left"
                    Text="{Binding InstallPath, Mode=OneWay}"/>
                <Separator
                    Margin="0 10 0 10"
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"/>
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    TextAlignment="Right"
                    Margin="0 0 5 0"
                    Text="{Binding Source={x:Static i18n:I18NSessions.OutputAutoDetectedLocFiles}, StringFormat={}{0}:, Mode=OneWay}"/>
                <ItemsControl
                    Grid.Row="2"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    VerticalContentAlignment="Top"
                    Margin="5 0 0 0"
                    ItemsSource="{Binding SessionManager.LocalizationFiles, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                Text="{Binding Name, Mode=OneWay}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Separator
                    Margin="0 10 0 10"
                    Grid.Row="3"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"/>
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    TextAlignment="Right"
                    Margin="0 0 5 0"
                    Text="{Binding Source={x:Static i18n:I18NSessions.InputSelectSession}, StringFormat={}{0}:, Mode=OneWay}"/>
                <ComboBox
                    Grid.Row="4"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    HorizontalContentAlignment="Left"
                    VerticalContentAlignment="Center"
                    IsEnabled="{Binding IsEnabled, Mode=OneWay}"
                    ItemsSource="{Binding SessionManager.TranslationSessions, Mode=OneWay}"
                    SelectedItem="{Binding SessionManager.CurrentTranslationSession, Mode=TwoWay}"
                    DisplayMemberPath="DisplayName"
                    Cursor="Hand"/>
                <Separator
                    Margin="0 10 0 10"
                    Grid.Row="5"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"/>
                <StackPanel
                    Grid.Row="6"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <!-- dont translate/localize CommandParameter -->
                    <ToggleButton
                        Command="{Binding CreateEditSessionCommand}"
                        CommandParameter="{x:Static sessionmodels:SessionActions.Create}"
                        IsChecked="{Binding SessionAction,
                                            Converter={StaticResource EnumBooleanConverter},
                                            ConverterParameter={x:Static sessionmodels:SessionActions.Create}}"
                        IsEnabled="{Binding IsEnabled, Mode=OneWay}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Margin="10 10 10 10"
                        Padding="5 5 5 5"
                        Cursor="Hand">
                        <StackPanel>
                            <Image
                                Source="/TranslateCS2;component/Assets/Icons/add.png"
                                MaxHeight="64"
                                MaxWidth="64"/>
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextAlignment="Center"
                                Text="{Binding Source={x:Static i18n:I18NSessions.DoCreate}, Mode=OneWay}"/>
                        </StackPanel>
                    </ToggleButton>
                    <!-- dont translate/localize CommandParameter -->
                    <ToggleButton
                        Command="{Binding CreateEditSessionCommand}"
                        CommandParameter="{x:Static sessionmodels:SessionActions.Edit}"
                        IsChecked="{Binding SessionAction,
                                            Converter={StaticResource EnumBooleanConverter},
                                            ConverterParameter={x:Static sessionmodels:SessionActions.Edit}}"
                        IsEnabled="{Binding IsEditEnabled, Mode=OneWay}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Margin="10 10 10 10"
                        Padding="5 5 5 5"
                        Cursor="Hand">
                        <StackPanel>
                            <Image
                                Source="/TranslateCS2;component/Assets/Icons/edit.png"
                                MaxHeight="64"
                                MaxWidth="64"/>
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextAlignment="Center"
                                Text="{Binding Source={x:Static i18n:I18NSessions.DoEdit}, Mode=OneWay}"/>
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
                <ContentControl
                    prism:RegionManager.RegionName="{Binding Source={x:Static app_configurationmanager:AppConfigurationManager.AppNewEditSessionRegion}, Mode=OneWay}"
                    Grid.Row="7"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Margin="5 5 5 5"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</ContentControl>
